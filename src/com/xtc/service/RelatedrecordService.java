package com.xtc.service;

import com.xtc.entity.Relatedrecord;

public interface RelatedrecordService {
	/**
	 * 添加物业信息
	 * @param related
	 * @return
	 */
	public boolean addRelatedrecord(Relatedrecord related);
}
